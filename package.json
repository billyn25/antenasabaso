{
  "name": "antenasabaso",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "node scripts/build.mjs && node scripts/audit.mjs",
    "audit": "node scripts/audit.mjs",
    "build:production": "node scripts/build.mjs --production && node scripts/audit.mjs --production",
    "test": "node --test tests/*.test.mjs"
  }
}
